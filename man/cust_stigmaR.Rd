% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cust_stigmaR.R
\name{cust_stigmaR}
\alias{cust_stigmaR}
\title{Build a Custom Sum-Score Composite Index}
\usage{
cust_stigmaR(df, state, year, cust_index, var_name, na_rm = FALSE)
}
\arguments{
\item{df}{A data frame containing your data.}

\item{state}{Character string. Name of the column in \code{df} containing
two-letter state abbreviations (e.g., "IL", "CA").}

\item{year}{Character vector. Years to merge in (e.g., \code{c("2016", "2017")}).
Each custom index is computed separately for each requested year and
added as a column named \code{YYYY_varname}.}

\item{cust_index}{A character vector of item names to sum into a single
custom index, or a list of such character vectors to build multiple
custom indices at once. Valid item names are determined automatically
from the columns of the bundled \code{items} dataset (every \verb{iat_sex_*} mean
column, excluding the \verb{iat_sex_n_*} respondent-count columns). See
\code{names(stigmaR::items)} for the full, current list.}

\item{var_name}{Character vector of names for the new column(s), one per
group in \code{cust_index} (i.e., \code{length(var_name)} must equal
\code{length(cust_index)}, or be \code{1} if \code{cust_index} is a single vector).}

\item{na_rm}{Logical. If \code{FALSE} (default), a composite is \code{NA} for a
given state-year if \emph{any} of its component items are \code{NA} for that
state-year. If \code{TRUE}, the composite is the sum of whatever component
items are non-missing (and \code{NA} only if \emph{all} are missing).}
}
\value{
The input data frame with new columns named \code{YYYY_varname}
containing the summed custom index for each requested year and
\code{var_name}.
}
\description{
Lets you combine individual items from the bundled \code{items} dataset into
your own sum-score composite(s), merged into your data by state and year.
Each composite is the row-wise sum of the items you choose, computed
within a single year (you cannot mix items from different years into one
composite). To merge individual items without combining them, see
\code{\link[=item_stigmaR]{item_stigmaR()}}.
}
\examples{
\dontrun{
# One custom index
new_df <- cust_stigmaR(
  df         = my_data,
  state      = "state_abbrev",
  year       = c("2016", "2017"),
  cust_index = c("iat_sex_imp_d", "iat_sex_exp_therm_gm"),
  var_name   = "my_custom_index"
)

# Multiple custom indices at once
new_df <- cust_stigmaR(
  df         = my_data,
  state      = "state_abbrev",
  year       = "2016",
  cust_index = list(
    c("iat_sex_imp_d", "iat_sex_exp_therm_gm"),
    c("iat_sex_exp_pol_marr", "iat_sex_exp_pol_legal", "iat_sex_exp_pol_adopt")
  ),
  var_name   = c("implicit_plus_therm", "policy_sum")
)
}

}
\references{
Kim, S. & Todd, N. R. (2027). stigmaR: Enhancing Accessibility,
Reproducibility, and Transparency of Structural Stigma Research.
}
