% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item_stigmaR.R
\name{item_stigmaR}
\alias{item_stigmaR}
\title{Calculate State-Level Individual Item Scores}
\usage{
item_stigmaR(df, state, item, year)
}
\arguments{
\item{df}{A data frame containing your data.}

\item{state}{Character string. Name of the column in \code{df} containing
two-letter state abbreviations (e.g., "IL", "CA").}

\item{item}{Character vector. One or more individual items to merge in.
The set of valid items is determined automatically from the columns of
the bundled \code{items} dataset (every \verb{iat_sex_*} mean column, excluding
the \verb{iat_sex_n_*} respondent-count columns). See
\code{names(stigmaR::items)} for the full, current list.}

\item{year}{Character vector. Years to merge in (e.g., \code{c("2016", "2017")}).
Each year x item combination becomes one new column named
\code{YYYY_itemname} (as an example).}
}
\value{
The input data frame with new columns named \code{YYYY_itemname}
containing matched state-level item scores for each requested year and
item.
}
\description{
Merges state- and year-level \emph{individual item} scores - the building
blocks behind the composite indices in \code{\link[=stigmaR]{stigmaR()}} - into your dataset.
Use this when you want full flexibility to work with individual items
(e.g., to build and compare your own composites). To create a single
summed composite column directly, see \code{\link[=cust_stigmaR]{cust_stigmaR()}}.
}
\details{
To keep merges manageable, a single call is limited to 100 new columns
(i.e., \code{length(item) * length(year)}). If you need more than that, split
the request into multiple calls.
}
\examples{
\dontrun{
new_df <- item_stigmaR(
  df    = my_data,
  state = "state_abbrev",
  item  = c("iat_sex_imp_d", "iat_sex_exp_therm_gm"),
  year  = c("2016", "2017")
)
}

}
\references{
Kim, S. & Todd, N. R. (2027). stigmaR: Enhancing Accessibility,
Reproducibility, and Transparency of Structural Stigma Research.
}
